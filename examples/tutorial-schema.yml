# Example schema for tutorial documentation
# Ensures step-by-step guides follow a consistent format

structure:
  - heading: "# .*" # Tutorial title
    children:
      - heading: "## Prerequisites"
        required_text:
          - "install"
      - heading: "## Step 1: *" # Step 1 with any subtitle
        code_blocks:
          - { min: 1 } # Each step needs at least one code example
      - heading: "## Step 2: *"
        code_blocks:
          - { min: 1 }
      - heading: "## Step 3: *"
        optional: true # Additional steps are optional
        code_blocks:
          - { min: 1 }
      - heading: "## Troubleshooting"
        optional: true
      - heading: "## Next Steps"
        optional: true
